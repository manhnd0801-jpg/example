# AI-GENERATED — Copy thành .env và điền giá trị thật
# KHÔNG commit file .env vào git

# URL của pbc-auth API
VITE_AUTH_API_URL=http://localhost:3001

# Tenant ID dùng khi chạy dev shell độc lập (npm run dev)
# Thay bằng tenant thật của môi trường dev của bạn
VITE_DEV_TENANT_ID=dev-tenant
