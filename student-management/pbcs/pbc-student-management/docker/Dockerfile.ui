# AI-GENERATED
# Multi-stage build cho pbc-student-management UI
FROM node:20-alpine AS builder
WORKDIR /app
COPY ui/package*.json ./
RUN npm install
COPY ui/ .
ARG VITE_API_URL=http://localhost:3002
ARG VITE_DEV_TENANT_ID=dev-tenant
ENV VITE_API_URL=$VITE_API_URL
ENV VITE_DEV_TENANT_ID=$VITE_DEV_TENANT_ID
RUN npm run build

FROM nginx:alpine AS runner
COPY docker/nginx.conf /etc/nginx/conf.d/default.conf
COPY --from=builder /app/dist /usr/share/nginx/html
EXPOSE 3012
HEALTHCHECK --interval=15s --timeout=5s --start-period=10s --retries=3 \
  CMD wget -qO- http://localhost:3012/nginx-health || exit 1
CMD ["nginx", "-g", "daemon off;"]
