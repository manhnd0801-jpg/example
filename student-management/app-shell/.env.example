# AI-GENERATED — Copy thành .env và điền giá trị thật

# Kafka brokers
VITE_KAFKA_BROKERS=localhost:9092

# Tenant ID mặc định cho môi trường dev
# Production: đọc từ subdomain hoặc JWT claim
VITE_TENANT_ID=dev-tenant
